VITE_REPORT_API_URL=https://vecindata-report-api-182638143110.us-central1.run.app
# VITE_GOOGLE_PLACES_API_KEY is intentionally NOT committed here — GitHub
# flags any Google API key string regardless of its HTTP-referrer
# restriction. Set it in the Cloudflare dashboard instead: the Worker's
# Settings > Build > "Build variables and secrets" (build-time, not the
# runtime "Environment variables" section — those aren't visible during
# `npm run build`). Vite picks up real process env vars automatically.
